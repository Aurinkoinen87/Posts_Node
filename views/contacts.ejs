<!DOCTYPE html>
<html lang="en">
  <%- include('./partials/header.ejs')%>

<body>
  <h1>Contacts page</h1>
  <ul>
    <% if(contacts.length){contacts.forEach(({name, link})=> { %>
      <li><a href=<%= link %>><%= name %></a></li>
    <%})}%>

  </ul>
</body>
</html>
